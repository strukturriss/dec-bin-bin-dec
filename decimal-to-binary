
function decBin (dec) {
  var targetArr = [];
  
  for (i=0; i <= Math.floor(Math.sqrt(dec)); i++) {
    if (dec-Math.pow(2,i) == 0) {
      targetArr.unshift(1);
      dec = dec-Math.pow(2,i);
    } else {
      targetArr.unshift(0);
    }
  }
  
  return targetArr;
}

decBin (128);
