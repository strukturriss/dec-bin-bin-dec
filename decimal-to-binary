
function decBin (dec) {
  var targetArr = [];
  for (i = dec; i >= 0; i--) {
    if (dec-Math.pow(2,i) < Math.pow(2,1)) {
      targetArr.push(1);
      dec = dec-(Math.pow(2,i));
    } else {
      targetArr.push(0);
      dec = dec-(Math.pow(2,i));
    }
  }
  return targetArr;
}

decBin (128);
